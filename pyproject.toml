[project]
name = "wishub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "load-dotenv>=0.1.0",
    "openai>=2.45.0",
    "zhipuai>=2.1.5.20250825",
]

[tool.uv.workspace]
members = [
  "backend"
]
